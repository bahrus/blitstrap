# blitstrap

This repository is an AI generated web component wrapper around the bootstap components.

This package chooses to forgo ShadowDOM for easier migration, but uses TypeScript for soure (but not equired for use).

